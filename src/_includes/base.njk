<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }} - Russ Johnson</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <header>
      <ul>
          {% for entry in collections.page %}
            <li>
                <a href="{{ entry.url }}"
                {% if entry.url == page.url %} aria-current="page" class="active"
                {% endif %}>
                {{ entry.data.title }}
                </a>
            </li>
        {% endfor %}
      </ul>
    </header>
    <main>
      {{ content | safe }}
    </main>
  </body>
</html>
